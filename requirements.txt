# Minimal requirements for Vercel deployment
# For full development dependencies, see requirements-full.txt or pyproject.toml

# FastAPI server for demo UI
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.12.5

# Minimal scientific computing (only numpy for market simulator)
numpy==2.4.0

# Core utilities
python-dateutil==2.9.0.post0

# Optional: QuestDB for time-series metrics logging
questdb==4.1.0

# Optional: ML dependencies for pattern detection
# To use ML features, set 'ml_enabled': true in config and run: python ml/train.py
scikit-learn>=1.5.0
joblib==1.3.2

# Optional: Production Dashboard dependencies (for Streamlit visualization)
# To install production dashboard dependencies:
# pip install streamlit plotly pandas
# 
# Production dashboards:
# - freqtrade/ui/prod_config.py - Configuration management
# - freqtrade/ui/prod_monitor.py - Real-time monitoring
streamlit>=1.31.0
plotly>=5.18.0
pandas>=2.1.0

# Optional: DSPy for LM-based insights in external analysis script
# To use DSPy insights: pip install dspy-ai ollama
dspy-ai==2.4.0
ollama==0.1.0

# Optional: Full stack for local development (commented out for Vercel)
# To install full dependencies locally:
# pip install -r requirements-full.txt
# or
# pip install -e .[dev]

